# 汽油數據整理規格

## 資料來源

- **範本**：`範例/油-汽油.xlsx`（每次從此複製，勿直接編輯）
- **PDF**：各據點的總分類帳汽油 PDF（例：`恩博格114汽油.pdf`）
- **輸出檔名**：`油-汽油_{民國年}{資料夾名}.xlsx`，存至對應據點資料夾（例：`油-汽油_114恩博格.xlsx`）

---

## 從 PDF 擷取的欄位

| 欄位 | 說明 |
|------|------|
| 月、日 | 傳票的月份與日期 |
| 摘要 | 僅保留摘要為**汽油**的列，排除其他品項（如衛生紙等） |
| 借方金額 | 對應該筆交易的借方金額（去除千分位逗號後為數字） |

---

## 填入 Excel 的規則

### 固定值

| 欄位 | 欄位名稱 | 值 |
|------|----------|----|
| A | \*據點編號 | `000` |

### 複製自第八列（範例列）

值與格式皆複製，套用至所有新增資料列。

| 欄位 | 欄位名稱 |
|------|----------|
| B | \*燃料代碼 |
| C | 活動數據單位代碼 |
| G | 品名 |

### 依資料填入

| 欄位 | 欄位名稱 | 規則 |
|------|----------|------|
| D | \*憑證日期 | 格式 `2025-mm-dd`（114年 = 2025年），依 PDF 月日填入 |
| I | \*金額 | 填入對應的借方金額（數字，無千分位逗號） |

### 不填值，但需複製第八列格式

| 欄位 | 欄位名稱 |
|------|----------|
| E | 單據號碼 |
| F | 買方統編 |
| H | \*公升數 |

---

## 格式複製規則

- 所有欄位（A～I）的第八列格式皆需套用至每一筆新增資料列
- 有填值的欄位（A、B、C、D、G、I）：值＋格式一起複製
- 不填值的欄位（E、F、H）：僅複製格式，值保持空白

---

## 注意事項

1. 從 `範例/油-汽油.xlsx` 複製範本後再填入，勿直接改動範本檔
2. 資料從第八列開始填入（範例列直接覆蓋）
3. 排除非汽油的摘要列（如「衛生紙等」、「本月合計」、「本期累計」等）
4. 憑證日期存為日期格式，顯示格式為 `yyyy-mm-dd`
5. 同一日期若有多筆汽油交易，每筆各填一列
6. 輸出檔存至對應據點資料夾，不要存回 `範例` 資料夾

---

## 上傳失敗根本原因與修補方法

### 問題描述

上傳系統報錯「上傳失敗，請檢查Excel格式或內容」，手動複製第八列格式到所有資料列後才能上傳成功。

### 根本原因

openpyxl 存檔時對 XLSX（ZIP 格式）的 sheet XML 做了三個不相容的改動：

1. **字串型別錯誤**：openpyxl 預設使用 `t="inlineStr"`（行內字串），但上傳系統要求 `t="s"`（shared strings，對應 sharedStrings.xml 的索引）。
2. **空白格型別錯誤**：空值儲存格被寫為 `<c ... t="n"></c>`，範例格式為自閉合 `<c .../>`。
3. **列層級格式屬性被覆寫**：`<row>` 元素的 `s="N" customFormat="1" ht="16" customHeight="1"` 屬性被 openpyxl 重設（電費範例 `s="7"` 被改為 `s="1"`），導致列格式失效。

### 修補方式

腳本在 openpyxl 存檔後，用 `zipfile` 直接讀取並修補 sheet XML：

- 將所有 `t="inlineStr"` 的字串值加入 sharedStrings.xml，改為 `t="s"` 索引引用
- 將 `<c ... t="n"></c>` 空白格改為自閉合 `<c .../>`
- 從原始範例的 row 8 讀取 `s/customFormat/ht/customHeight` 屬性，補回所有資料列
- 補回 `[Content_Types].xml` 的 sharedStrings Override 宣告，以及 `xl/_rels/workbook.xml.rels` 的 sharedStrings Relationship（openpyxl 存檔時若無 shared strings 會一併移除這兩筆宣告，導致 Excel 找不到 sharedStrings.xml，所有字串格被清空）

此修補由 `fix_xlsx_for_upload()` 函式執行，已整合於 `gasoline_process.py` 和水電瓦斯 `process.py` 中，不需手動操作。
